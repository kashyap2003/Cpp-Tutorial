#include <iostream>
using namespace std;

int main(void)
{
    int a,b;
    cout << "Enter two no. to get there division" << endl;
    cout << "A: ";
    cin >> a;
    cout << "B: ";
    cin >> b;
    cout << (float)a/b << endl;
    cout << "NOTE: 1st no. gets divide by 2nd one\nIn this formate you have to write any integer to result in real no.\nSo, I really think this is a stupid method so plz refer division1 that is better than this(division2).";

    
}