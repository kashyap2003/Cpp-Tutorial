// Structure in C++

// NOTES:
// Structure in C++ is similar with class in C++, except by default access specifier in classes is private
// but public in structure.

// Example 2:
// If not declared while inheriting

class A{};
class B: A{};

struct A{};
struct B: A{};